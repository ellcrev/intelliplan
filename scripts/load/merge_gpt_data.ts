import { AllDataJSON } from "../../lib/meilisearch/types";

/**
 * This function merges the most recent GPT
 * data available in the data/gpt folder with
 * the currently parsed data in data/parsed folder.
 */
const mergeGPTData = (allData: AllDataJSON) => {
  return allData;
};

export default mergeGPTData;
